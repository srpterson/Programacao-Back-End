# Projeto B - ES Modules

Neste projeto foi utilizado o sistema de módulos ECMAScript Modules.

Características:

- `"type": "module"` no `package.json`;
- importação com `import`;
- exportação com `export`.
